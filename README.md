# Py-AST-Mutator

This tool is a mutation testing tool to be used with small Python projects that use Pytest.

The tool utilizes ASTs to parse souce code to change operators one at a time.

You will need pytest install to run the tool.

To use the tool, simply run the following: python main.py <input file or directory>
